# Dev Notes

Each game page is fully self-contained: its own `index.html`, CSS, and
JS under `games/<name>/`. Shared code (`css/style.css`,
`js/scoreboard.js`) lives at the repo root and is included via
relative paths.
